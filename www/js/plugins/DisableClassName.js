Window_Base.prototype.drawActorClass = function(actor, x, y, width) {
 //   if (disableClassName) return;
    width = width || 168;
    this.resetTextColor();
 //   this.drawText(actor.currentClass().name, x, y, width);
};
